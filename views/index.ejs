      filter: drop-shadow(0 2px 6px rgba(59,130,246,0.25));
    }

    /* ── Responsive ── */
    @media (max-width: 600px) {
      nav { display: none; }
      .header-inner { padding: 14px 16px; }
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <div class="header-inner">
      <!-- Logo (right side in RTL) -->
      <a href="#" class="logo">
        <span class="logo-s">S</span>
        <span class="logo-ar">صفي</span>
        <span class="logo-emoji">📚</span>
      </a>

      <!-- Navigation (center) -->
      <nav>
        <a href="#" class="active">الرئيسية</a>
        <a href="#">من نحن</a>
        <a href="#">اتصل بنا</a>
      </nav>

      <!-- Login button (left side in RTL) -->
      <button class="btn-login">تسجيل الدخول</button>
    </div>
  </header>

  <!-- Hero -->
  <section class="hero">
    <h1>مرحباً بك في صفي</h1>
    <p>منصتك التعليمية السهلة</p>
  </section>

  <!-- Grade Card -->
  <div class="card-wrapper">
    <div class="card">
      <h2>عاشر عام</h2>
      <p class="sub">ابدأ رحلتك التعليمية معنا</p>

      <button class="btn-start">ابدأ الآن</button>

      <!-- Book + Pencil SVG icon -->
      <svg class="book-icon" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
        <rect x="8"  y="10" width="40" height="44" rx="4" fill="#3B82F6"/>
        <rect x="10" y="12" width="36" height="40" rx="3" fill="#60A5FA"/>
        <rect x="8"  y="10" width="8"  height="44" rx="4" fill="#2563EB"/>
        <rect x="22" y="22" width="20" height="2.5" rx="1.25" fill="white" opacity="0.8"/>
        <rect x="22" y="28" width="16" height="2.5" rx="1.25" fill="white" opacity="0.6"/>
        <rect x="22" y="34" width="18" height="2.5" rx="1.25" fill="white" opacity="0.6"/>
        <text x="32" y="48" text-anchor="middle" font-size="7" fill="white"
              font-family="Cairo, sans-serif" font-weight="700" opacity="0.9">صفي</text>
        <g transform="translate(38,38) rotate(-30)">
          <rect x="0" y="0" width="6" height="18" rx="1" fill="#FBBF24"/>
          <polygon points="0,18 6,18 3,24" fill="#F59E0B"/>
          <rect x="0" y="0" width="6" height="3"  rx="1" fill="#9CA3AF"/>
          <rect x="1" y="3" width="4" height="1.5" fill="#F3F4F6"/>
        </g>
      </svg>
    </div>
  </div>

</body>
</html>
